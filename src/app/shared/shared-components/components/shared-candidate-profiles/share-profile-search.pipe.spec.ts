import { ShareProfileSearchPipe } from './share-profile-search.pipe';

describe('ShareProfileSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ShareProfileSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
